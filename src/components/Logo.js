import React from 'react';


const Logo = () => {
    return (
        <div>
            {/* Les images importées depuis la balise IMG sont accessibles dans "public" */}
            <h3>Skan's World</h3>
        </div>
    );
};

export default Logo;